# Dependencies & build output
node_modules/
dist/
build/
out/
*.tsbuildinfo

# inplan runtime sidecars (per-document logs, backups, proposals)
.inplan/
# legacy sidecar dir name (pre-rename) — keep ignored so stale backups never return
.agent-planner/

# Playwright e2e artifacts
test-results/
playwright-report/
.playwright-mcp/

# OS / editor cruft
.DS_Store
*.log

# Local Claude Code config (not part of the product)
.claude/settings.json
release/

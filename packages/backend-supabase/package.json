{
  "name": "@inplan/backend-supabase",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase (Postgres + Realtime + Storage) implementation of the inplan ControlChannel / DocumentStore seam — an authenticated client, AGPL.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inplan/core": "file:../../../inplan/packages/core",
    "@supabase/supabase-js": "^2.45.0"
  }
}
